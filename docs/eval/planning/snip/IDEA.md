# Idea

A small CLI tool that lets a developer save, list, and re-run shell command
snippets by name — so they stop re-typing the same long, fiddly commands.

<!-- This is a deliberately loose, under-specified idea: the input the planning
     stage (tiny-spec-prd → tiny-spec-breakdown) is supposed to expand. The eval
     does not prescribe the PRD's contents; it grades conformance, hand-off
     integrity (coverage + no fabrication), and atomicity of the produced
     PRD.md / BREAKDOWN.md. -->
